[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "indic-hallucination-eval"
version = "0.1.0"
description = "A Python library and API for evaluating LLM hallucinations in Indic languages."
